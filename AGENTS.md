# Store implementation continuity

Read `DIMENSIONAL-STORE-TRAVEL-STANDARD-0.1.md` and `D001-MACHINE-IMPLEMENTATION-0.1.md` before changing capability, travel, pricing or consumers.

Preserve existing project definitions, catalog-driven selection, actor boundaries and downstream records. Extend the existing Store evaluator; do not create project-specific surrogate answers or copy Store logic into an app. Machine roles, admitted capability, calculable time and physical commissioning are distinct facts.

The three-router/two-spot inventory has recovered source provenance. Its unresolved bindings and tooling must not be guessed. The historical centered-spot price is not evidence for the requested full-diameter depth. Do not replace missing geometry with an assumed angle, old plunge time, setup charge or hourly placeholder.

Sensing is proposed coordination, not commissioned observation or safety authority. Store and application outputs never authorize physical execution.

Test real exported consumer behavior, including non-first features, boundary values, missing facts and recognized unsupported demands. Run `node --test *.test.mjs`. Do not change expected outcomes merely to obtain green tests. Report whether an exact tested Store revision has actually been adopted by runtime consumers; a pushed branch is not a deployed app.
